# Horsehood Whitelist Site

Static, framework-free whitelist landing page for Horsehood.

## Deploy

Upload this folder's contents to a GitHub repository, then enable **GitHub Pages** from the repository settings. Set the source to the `main` branch and the root (`/`) folder.

No build step or dependencies are required: GitHub Pages serves `index.html` directly.
